function FizzBuzz (...args){
    let multiplos = 3;

    args.forEach((arg) => multiplos += arg);
    return multiplos;
}

function convert(param1, param2){
    for(let i = 0; i <= 100; i++) {
        if(i % param1 === 0 && i % param2 === 0 ) {

        } else if(i % param1 === 0) {

        } else if(i % param2 === 0) {

        } else {
            
        }
    }
}
