const series = [
    "Lista Negra",
    "Stranger Things",
    "Better Call Saul",
    "SuperNatural",
    "Wandinha",
    "Peaky Blinkers"
];

//const [wandinha, listaNegra] = [series[4], series[0]];

//const wandinha = series[4];
//const listaNegra = series[0];

const [
    listaNegra,
    strangerThings,
    betterCallSaul,
    superNatural,
    wandinha,
    peakyBlinkers
] = series;

//console.log(wandinha);
//console.log(listaNegra);
console.log(series);

console.log(series[series.length - 1]);

if(series[series.length -1]){
    if(typeof series[series.length -1] === "string"){
        
    }
}